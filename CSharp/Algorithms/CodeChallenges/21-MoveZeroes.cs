namespace Algorithms.CodeChallenges
{
    /***
    * given an array nums, write a function to move all 0's to the end of it while maintaining the relative order of the nun-zero elements
    * input: [0,1,0,3,12]  output: [1, 3, 12, 0, 0]
    * you must do this in-place without makin a copy of the array
    * minimize the total number of operations
    ***/
    public static class MoveZeroes
    {
        public static void Execute(){
            // Console.WriteLine($"Factorial of 5 is: {factorial(5)}");
            // Console.WriteLine($"Factorial of 5 (recursive) is: {factorialRecursive(5)}");
        }

        private static void moveZeroes(int[] n) {
            
        }
    }
}